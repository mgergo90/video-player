/*
 * VideoFilter Messages
 *
 * This contains all the text for the VideoFilter container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VideoFilter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoFilter container!',
  },
});
